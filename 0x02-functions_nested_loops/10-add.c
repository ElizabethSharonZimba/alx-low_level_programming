#include <stdio.h>
#include "main.h"
/**
 * add - adds 2 int & returns the res
 * @a: num being added
 * @b: num being added
 * Return: (0) indicates successful execution
 */
int add(int a, int b)
{
	int res;

	res = a + b;
	return (res);
}
