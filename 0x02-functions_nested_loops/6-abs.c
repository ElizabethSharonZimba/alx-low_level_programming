#include <stdio.h>
#include "main.h"
/**
 * _abs - computers Absolute value of an int
 * @a: The int to check
 * Return: The Absolute value of an int
 */
int _abs(int a)
{
if (a >= 0)
return (a);
return (-a);
}

